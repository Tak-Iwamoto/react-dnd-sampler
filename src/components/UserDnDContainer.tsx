// import { FC, useRef } from 'react';
// import { useDrag } from 'react-dnd';
// import { User } from '../data/user';

// type Props = {
//   index: number;
//   field: User;
//   move: (dragIndex: number, hoverIndex: number) => void;
// };

// export const UserDnDContainer: FC<Props> = ({ children, index, field, move }) => {
//   const ref = useRef(null);

//   const { name } = field;

//   const [, drag] = useDrag({
//     item: { name, index, type: Dr },
//   });
// };
